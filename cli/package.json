{
  "name": "claude-notify",
  "version": "1.0.0",
  "description": "Push notifications when Claude Code finishes on SSH servers",
  "bin": {
    "claude-notify": "./bin/claude-notify.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "qrcode-terminal": "^0.12.0"
  }
}
